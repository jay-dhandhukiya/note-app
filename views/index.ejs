<!DOCTYPE html>
<html lang="en">
<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Document</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
     <div class="main w-full h-screen bg-zinc-900">
          <div class="w-full px-10 py-8">
               <form action="/create" method="post">
                    <input class="text-white bg-zinc-800 px-4 py-2 w-full rounded-lg" type="text" name="title" placeholder="Title of the note">
                    <textarea class="text-white bg-zinc-800 px-4 py-2 w-full mt-4 resize-none rounded-lg" rows="4" name="note" placeholder="Write your note"></textarea>
                    <button id="submit" class="mt-4 text-white bg-blue-500 px-5 py-2 rounded-md" name="submit" type="submit">Submit</button>
               </form>
          </div>
          <div class="w-full px-10 py-8 flex gap-4 flex-wrap">
               <% if (files.length > 0) { %>
               <%   files.forEach(function(val){ %>
                    <div class="w-76 h-24 py-4 bg-zinc-800 px-4 py-2 rounded-lg">
                         <h1 class="text-white text-2xl font-bold"><%= val %></h1>
                         <div class="flex gap-4 justify-between mt-3">
                              <a class="text-blue-500 text-sm font-bold" href="/files/<%= val %>">Read more.</a>
                              <a class="text-blue-500 text-sm font-bold" href="/delete/<%= val %>">Delete</a>     
                              <a class="text-blue-500 text-sm font-bold" href="/edit/<%= val %>">Edit</a>
                         </div>
                    </div>     
               <%   }) %>
               <% } else { %>
                    <h3 class="text-white">No notes found.</h3>
               <% } %>
          </div>
     </div>
</body>
</html>